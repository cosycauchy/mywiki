# CLAUDE.md

이 저장소에서 작업할 때 참고할 컨텍스트. 이전 세션(Claude Cowork)에서 여기까지 만들었다.

---

## 프로젝트

나무위키 스타일 **개인 아카이브 위키**. `docs/*.namu` (나무마크) → 정적 HTML → GitHub Pages.

- 라이브: https://cosycauchy.github.io/mywiki/
- 저장소: `cosycauchy/mywiki` (branch: `main`)
- 서버·DB 없음. 파이썬 표준 라이브러리만 사용 (외부 패키지 0개, 설치할 것 없음)
- 문서는 현재 **`joohwan` 하나뿐**이고, 앞으로도 인물 문서는 이것만 유지한다. 다른 사람 문서는 만들지 않는다.

## 명령어

```bash
python build.py                              # docs/*.namu → dist/
python -m http.server 8000 --directory dist  # http://localhost:8000 미리보기
python make_preview.py                       # dist/ 전체를 preview.html 한 파일로 묶음 (선택)
```

배포는 `main`에 push하면 `.github/workflows/deploy.yml`이 빌드 후 Pages에 올린다. 반영까지 1~2분.

## 파일 구조

```
mywiki/
├── build.py                      빌더 (전부 여기 들어있음, ~700줄)
├── config.json                   사이트 이름 / 대문 문서 / 저장소 주소
├── make_preview.py               dist/를 단일 HTML로 번들 (공유·확인용)
├── docs/
│   └── joohwan.namu              ← 실질적으로 유일한 콘텐츠 파일
├── dist/                         빌드 결과. .gitignore 됨, 절대 커밋하지 않는다
├── README.md                     사용자용 문법·운영 가이드
└── .github/workflows/deploy.yml
```

### config.json

```json
{
  "site_name": "주환위키",
  "front_page": "joohwan",
  "github_repo": "cosycauchy/mywiki",
  "github_branch": "main"
}
```

- `front_page`에 적힌 문서가 **사이트 루트(`/index.html`)에 통째로 한 번 더 렌더링**된다. 리다이렉트가 아니라 실제 HTML 복사본이라 깜빡임이 없다.
- `github_repo`가 채워져 있어야 문서 상단에 **✏ 편집 / 역사** 버튼이 뜬다 (GitHub 편집 화면으로 링크).

## build.py 구조

| 함수 | 역할 |
|---|---|
| `slug(name)` | 문서 이름 → 디스크 파일명. 공백만 `_`로, **한글은 그대로 유지** |
| `enc(s)` | 파일명 → URL용 퍼센트 인코딩 |
| `doc_url` / `cat_url` | 링크에 넣을 상대 경로 |
| `inline(text, ctx)` | 인라인 문법 (링크, 각주, 굵게, 백틱 코드…) |
| `parse(src, ctx)` | 블록 문법 (문단, 표, 목록, 인용, 코드블록, 접기, 인포박스) → `(html, toc, has_toc)` |
| `page(...)` | HTML 셸 (헤더/검색창/다크모드 토글/푸터) |
| `build()` | 전체 빌드 |
| `CSS` / `JS` | 문자열 상수. 스타일·클라이언트 스크립트를 고치려면 여기 |

출력물: `dist/index.html`(대문), `dist/w/*.html`(문서), `dist/c/*.html`(분류), `dist/all.html`(전체 목록), `dist/search.json`(검색 인덱스), `style.css`, `app.js`, `.nojekyll`

### 렌더링 파이프라인의 주의점

`inline()`은 **플레이스홀더 방식**으로 동작한다. 링크·각주·코드 같은 조각을 `\x00LIT{n}\x00` 마커로 치환해 `stash`에 넣어두고, 전체를 `html.escape` 한 뒤 `'''굵게'''` 류 정규식을 돌리고, 마지막에 마커를 복원한다.

새 인라인 문법을 추가할 때는 **반드시 이 순서를 지켜야 한다.** 마커로 감싸지 않으면 escape나 이후 정규식에 내용이 훼손된다. `\x01BR\x01`은 문단 내 줄바꿈용 별도 마커다.

## 지원 문법

구현됨:

```
[목차]                          목차 자동 생성 + 문단 번호
== 문단 ==                      === / ==== 로 하위 단계
'''굵게''' ''기울임'' __밑줄__ ~~취소선~~
[[문서]]  [[문서|보일 이름]]     없는 문서는 빨간 링크(.not-exist)
[[https://주소|이름]]           외부 링크
[[분류:이름]]                    분류 지정 (분류 페이지 자동 생성)
[* 각주]                        문서 하단에 수집
 * 목록  /  1. 번호 목록         앞 공백 수로 중첩 단계 결정
|| 표 || '''헤더는 굵게''' ||
> 인용문
----                            수평선
`인라인 코드`                    백틱 안은 문법 처리 안 함 (이스케이프용)
{{{ 코드블록 }}}
{{{#!folding [제목] ... }}}      접기
{{{#!infobox 제목 ... }}}        우측 플로팅 인포박스. 안에 || 키 || 값 ||, --- 로 구분선
{{{#색 텍스트}}}                 색 지정
```

미구현: 이미지/파일 첨부, 리다이렉트, 역링크 목록, 표 셀 병합, 문단 접기, 수식.

## 함정 (실제로 겪은 것들)

1. **한글 파일명을 퍼센트 인코딩된 상태로 디스크에 쓰면 안 된다.** 초기 버전이 `주환.html` 대신 `%EC%A3%BC%ED%99%98.html`이라는 이름의 파일을 만들어서 전부 404가 났다. 웹서버가 요청 URL을 디코드해서 찾기 때문. `slug()`는 한글을 그대로 두고, `enc()`는 링크에만 쓴다.

2. **윈도우에서 zip을 풀면 한글 파일명이 깨진다.** `docs/문서 목록.namu`가 `臾몄꽌 紐⑸줉.namu`가 된 적이 있다. 파일을 주고받을 때 주의.

3. **`dist/`는 커밋하지 않는다.** GitHub Actions가 서버에서 빌드한다.

4. **로컬 폴더가 아직 git 저장소가 아니다** (`.git` 없음). 지금까지는 GitHub 웹 업로드로 배포했다. git으로 전환하려면:

   ```bash
   git init && git branch -M main
   git remote add origin https://github.com/cosycauchy/mywiki.git
   git fetch origin
   git reset --soft origin/main   # 원격 이력만 붙이고 작업 파일은 그대로
   git add . && git commit -m "..." && git push
   ```

5. 표 셀 안에 `||`를 쓸 수 없다. 파서가 구분자로 먹는다. 문법 설명 등은 목록으로 우회할 것.

## 콘텐츠 규칙

- **톤: 나무위키 특유체.** `~이다` 말투, 사실을 나열하되 한 번씩 능청스럽게 짚기, 각주로 딴지 걸기. 담백한 백과사전체가 아니다.
- **개인정보는 대략적으로만.** 사이트가 공개 상태라서 주소를 아는 사람은 들어올 수 있다. 생년월일 대신 "2004년 11월", 상세 주소 대신 "대전광역시 유성구" 수준을 유지한다. (이미 적힌 학교명·복무 이력은 본인 승인 하에 들어간 것)
- 모든 페이지에 `noindex,nofollow`가 박혀 있어 검색엔진에는 안 잡힌다.
- 추측성 서술은 각주로 "본인 확인 필요"를 달아둔다. 현재 성격 문단에 하나 있다.
- 내용은 **본인에게 질문 → 답변 → 문체 입혀서 서술**하는 방식으로 채워왔다. 없는 사실을 지어내지 않는다.

## 문서 현황 (joohwan.namu)

인포박스 + 개요 / 생애(창원 시절·KAIST 입학과 기계공학·군 복무·현재) / 성격 / 취향(음식·음악·스포츠·취미) / 말버릇 / 여담.

핵심 서사는 **"화학·생물 전공 → 기계공학으로의 회귀"**. 자작자동차 동아리 '질주' 활동이 전환점.

### 남은 작업

- [ ] 여담 문단 보강 (현재 4개 + `※ 추가 예정` 플레이스홀더)
- [ ] 성격 문단의 "ADHD끼 vs 규칙적 생활 집착" 서술 — 본인 확인 후 각주를 떼거나 문장을 삭제
- [ ] 비공개 전환 검토 (Cloudflare Pages + Zero Trust Access, 무료 플랜으로 가능)
- [ ] 이미지 지원 — 인포박스에 사진을 넣으려면 필요
